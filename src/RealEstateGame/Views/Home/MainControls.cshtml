@using (Html.BeginForm("action", "Home", FormMethod.Post))
{
    <div>
        <button class="btn btn-success" name="selectedAction" value="overtime" onclick="this.form.submit()">$$ Work Overtime $$</button>
    </div>
}
